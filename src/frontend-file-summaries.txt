{
  "frontend/src/App.tsx": "Main app component that orchestrates asset validation for \"-1\" suffixed assets with refresh and resync capabilities, displays the asset scan results overlay after successful validation (requiring user acknowledgment via Continue button), resets acknowledgment state on refresh to force re-gating, renders the full-screen cinematic theatre scene with layered components and always-available PreviewSyncControls (including publish handler) only after the user proceeds.",
  "frontend/src/hooks/useRequiredAssets.ts": "Custom hook that validates the existence of all required \"-1\" suffixed image assets on startup and exposes a refresh function that triggers a new validation pass with cache-busting fetch options and timestamp query parameters, returning the first missing filename or null if all assets are present along with per-asset validation results.",
  "frontend/src/components/MissingAssetOverlay.tsx": "Full-screen blocking overlay component that displays the exact error message \"MISSING ASSET: [filename] — DO NOT SUBSTITUTE\" with optional \"Refresh Assets\" and \"Resync Preview\" action buttons when callbacks are provided.",
  "frontend/src/components/BackgroundSilhouetteLogo.tsx": "Renders a huge, low-opacity, non-tiled silhouette of logo2-1.png at 160% viewport width in the background layer behind all other scene elements.",
  "frontend/src/components/TheatreStageScene.tsx": "Full-screen cinematic theatre/stage environment with pure black base, heavy curtains with folds, subtle seating rows, central aisle with warm lights, overhead truss with hanging spotlights, and subtle volumetric light beams.",
  "frontend/src/components/CenteredHubLogo.tsx": "Fixed centerpiece component that displays logo2-1.png as a large dominant element at exact viewport center with a very faint halo/orbit ring around it, layered above the theatre scene, with a deterministic two-phase render mechanism that unmounts and remounts the image element on mount for asset rebinding.",
  "frontend/src/index.css": "Global styles with OKLCH color system, full-height viewport setup for immersive theatre experience, and custom radial gradient utility.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file for future React Query hooks (currently empty as STEP 1 requires no backend queries).",
  "frontend/src/components/AssetScanResultsOverlay.tsx": "Full-screen blocking overlay component that displays a list of all validated required assets with success indicators, a \"Refresh Assets\" button, a \"Resync Preview\" button (when callbacks provided), and a Continue button to proceed to the theatre scene.",
  "frontend/src/components/PreviewSyncControls.tsx": "Floating control component (bottom-right) that provides Refresh Assets, Resync Preview, and Publish actions via a popover menu, with a publish dialog that validates/sanitizes the slug using the publishSlug utility and shows constraint errors before allowing publish.",
  "frontend/src/utils/publishSlug.ts": "Utility module that validates and sanitizes publish slugs by removing prefixes/paths, converting disallowed characters to hyphens, and enforcing the 5-50 character alphanumeric-or-hyphen constraint with clear English error messages, including a getDefaultPublishSlug function that generates valid slugs from hostname or fallback values.",
  "frontend/src/config.ts": "Application configuration module that loads settings from env.json and provides typed access to Internet Identity derivation origin configuration."
}